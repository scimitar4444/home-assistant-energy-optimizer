[project]
name = "home-assistant-energy-optimizer"
version = "0.1.0b2"
description = "Deterministic 48-hour energy optimizer for Home Assistant"
requires-python = ">=3.13"
license = "MIT"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
