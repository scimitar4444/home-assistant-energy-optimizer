# Security Policy

Please report security issues privately through GitHub Security Advisories instead of opening a public issue. Do not include Home Assistant tokens, public URLs, IP addresses, entity dumps or inverter credentials in reports.

The integration performs no remote telemetry. Optional Victron Modbus TCP calls stay inside the network configured by the user.
